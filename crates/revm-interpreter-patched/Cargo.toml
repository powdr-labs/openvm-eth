# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.88.0"
name = "revm-interpreter"
version = "32.0.0"
authors = ["Dragan Rakita <dragan0rakita@gmail.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Revm Interpreter that executes bytecode."
readme = "README.md"
keywords = [
    "revm",
    "evm",
    "ethereum",
    "blockchain",
    "no_std",
]
license = "MIT"
repository = "https://github.com/bluealloy/revm"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[features]
arbitrary = [
    "std",
    "primitives/arbitrary",
]
default = ["std"]
hashbrown = ["primitives/hashbrown"]
memory_limit = []
# Runtime "JIT" basic-block transpiler: at each EVM opcode, check the
# bytecode bytes against known patterns; on hit, dispatch to a transpiled
# RV32IM handler. No build-time table needed.
evm-jit = []
# Build-time AOT basic-block transpiler: each (code_hash, pc) that matches
# a known pattern is precomputed offline and embedded as a const lookup
# table. At runtime, step() consults a per-contract u16 dispatch table.
evm-aot = []
# Build-time AOT for every pure basic block in the corpus (generic — not
# tied to named patterns). Emits a per-contract dispatch table mapping
# `(code_hash, pc)` -> `BbDesc`. step() invokes a single generic walker.
evm-bb-aot = []
serde = [
    "dep:serde",
    "primitives/serde",
    "bytecode/serde",
    "context-interface/serde",
    "state/serde",
]
std = [
    "serde?/std",
    "primitives/std",
    "context-interface/std",
    "bytecode/std",
    "state/std",
    "serde_json/std",
]

[lib]
name = "revm_interpreter"
path = "src/lib.rs"

[dependencies.bytecode]
version = "8.0.0"
default-features = false
package = "revm-bytecode"

[dependencies.context-interface]
version = "14.0.0"
default-features = false
package = "revm-context-interface"

[dependencies.primitives]
version = "22.0.0"
default-features = false
package = "revm-primitives"

[dependencies.serde]
version = "1.0"
features = [
    "derive",
    "rc",
    "derive",
    "rc",
]
optional = true
default-features = false

[dependencies.state]
version = "9.0.0"
default-features = false
package = "revm-state"

[dev-dependencies.serde_json]
version = "1.0"
features = ["alloc"]
default-features = false

[lints.rust]
missing_debug_implementations = "warn"
missing_docs = "warn"
unreachable_pub = "warn"
unused_must_use = "deny"

[lints.rust.rust_2018_idioms]
level = "deny"
priority = -1

[lints.rustdoc]
all = "warn"
