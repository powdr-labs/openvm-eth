[package]
name = "openvm-reth-benchmark"
edition.workspace = true
repository.workspace = true
homepage.workspace = true
exclude.workspace = true
version.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
url.workspace = true
tracing-subscriber.workspace = true
tracing.workspace = true
dotenv = "0.15.0"
clap = { version = "4.5.7", features = ["derive", "env"] }
clap_builder = "4.5.34"
bincode = { workspace = true, features = ["std"] }
bitcode.workspace = true
hex = "0.4.3"
serde_json.workspace = true
eyre.workspace = true
tokio.workspace = true
zstd.workspace = true
toml.workspace = true

# workspace
openvm-stateless-executor.workspace = true
openvm-rpc-proxy.workspace = true

# alloy
alloy-primitives.workspace = true
alloy-provider.workspace = true
alloy-rpc-client.workspace = true
alloy-transport.workspace = true
alloy-transport-http.workspace = true

# reth
reth-primitives.workspace = true

# Note: the openvm-sdk commit does not need to be exactly the same as the `openvm` commit used in the guest program: as long as
# the openvm-sdk commit doesn't change any guest libraries, they are compatible
# This allows us to not update revm and openvm-kzg each time we change openvm-sdk
openvm-stark-sdk = { git = "https://github.com/openvm-org/stark-backend.git", branch = "develop-v2", default-features = false }
openvm-cuda-backend = { git = "https://github.com/openvm-org/stark-backend.git", branch = "develop-v2", default-features = false, optional = true }
openvm-transpiler = { git = "https://github.com/openvm-org/openvm.git", branch = "develop-v2.0.0-rc.1", default-features = false }
openvm.workspace = true
openvm-circuit = { git = "https://github.com/openvm-org/openvm.git", branch = "develop-v2.0.0-rc.1", default-features = false }
openvm-verify-stark-host = { git = "https://github.com/openvm-org/openvm.git", branch = "develop-v2.0.0-rc.1", default-features = false }
openvm-sdk = { git = "https://github.com/openvm-org/openvm.git", branch = "develop-v2.0.0-rc.1", default-features = false }
openvm-sdk-config = { git = "https://github.com/openvm-org/openvm.git", branch = "develop-v2.0.0-rc.1", default-features = false }

# halo2
halo2-axiom = { version = "0.5.0", default-features = false, optional = true }

[package.metadata.cargo-machete]
ignored = ["halo2-axiom"]

[features]
default = ["parallel", "jemalloc", "metrics"]
parallel = ["openvm-sdk/parallel"]
metrics = ["openvm-sdk/metrics"]
tco = ["openvm-sdk/tco"]
aot = ["openvm-sdk/aot"]
evm-prove = ["openvm-sdk/evm-prove", "halo2-axiom"]
evm-verify = ["evm-prove", "openvm-sdk/evm-verify", "halo2-axiom"]
perf-metrics = ["openvm-sdk/perf-metrics"]
mimalloc = ["openvm-sdk/mimalloc"]
jemalloc = ["openvm-sdk/jemalloc"]
# x86 only:
halo2-asm = ["halo2-axiom?/asm"]
cuda = ["dep:openvm-cuda-backend", "openvm-sdk/cuda"]
unprotected = ["openvm-sdk/unprotected"]
nvtx = ["openvm-stark-sdk/nvtx"]
